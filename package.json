{
  "name": "khodakarami-services",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && cd dist && git init && git add . && git commit -m \"deploy\" && git push -f https://github.com/ardi-the-water/mykh.git master:gh-pages"
  },
  "dependencies": {
    "astro": "^4.11.5"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
